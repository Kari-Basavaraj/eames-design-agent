# Eames Design Agent - Environment Variables
# Copy to .env and fill in your keys, OR let Eames prompt you on first run.
# API keys entered via the TUI are saved to ~/.eames/config.json (global).

# Required: Anthropic API key (get at https://console.anthropic.com/settings/keys)
ANTHROPIC_API_KEY=

# Optional: Additional providers (for /model switching)
OPENAI_API_KEY=
GOOGLE_API_KEY=

# Optional: OpenRouter (for using OpenAI/Google models in SDK mode)
# Get at https://openrouter.ai/settings/keys
OPENROUTER_API_KEY=

# Optional: Tavily web search (for LangChain research mode)
TAVILY_API_KEY=

# Optional: LangSmith tracing (debugging)
# LANGSMITH_API_KEY=
# LANGSMITH_TRACING=false
