# LLM API Keys
OPENAI_API_KEY=your-api-key
ANTHROPIC_API_KEY=your-api-key
GOOGLE_API_KEY=your-api-key

# Auto-fallback: if Anthropic fails (billing, 403), Eames retries with OpenAI.
# Set OPENAI_API_KEY to use this. No extra flag needed.

# SDK mode (default): EAMES_USE_LANGCHAIN=1 to use LangChain instead.
# SDK supports Anthropic direct (ANTHROPIC_API_KEY) or OpenAI via OpenRouter.
# For OpenAI models in SDK: set OPENROUTER_API_KEY (or OPENAI_API_KEY), add key at openrouter.ai/settings

# Ollama (Local LLM)
OLLAMA_BASE_URL=http://127.0.0.1:11434

# Stock Market API Key
FINANCIAL_DATASETS_API_KEY=your-api-key

# Web Search API Key
TAVILY_API_KEY=your-api-key

# LangSmith (traces/debugging). Set to false to stop "403 multipart" errors when key is missing.
LANGSMITH_API_KEY=your-api-key
LANGSMITH_ENDPOINT=https://api.smith.langchain.com
LANGSMITH_PROJECT=eames-design-agent
LANGSMITH_TRACING=false

# PostgreSQL (for LangGraph checkpointing, Phase 0+)
DATABASE_URL=postgresql://postgres:eames@localhost:5432/eames
