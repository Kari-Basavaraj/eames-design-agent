import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  display: 'swap',
  variable: '--font-inter',
});

export const metadata: Metadata = {
  title: "Eames AI - Autonomous Product Design Agent",
  description: "Transform ideas into production-ready applications. From initial concept to live deployment on Vercel in under 15 minutes.",
  keywords: ["AI", "product design", "autonomous agent", "code generation", "deployment"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
